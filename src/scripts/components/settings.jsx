(function(undefined) {
    'use strict';

    var React = require('react');

    var Settings = React.createClass({
        render: function() {
            return (
                <div>TODO: Settings</div>
            );
        }
    });

    module.exports = Settings;
})();
