(function(undefined) {
    'use strict';

    var React = require('react');

    var Login = React.createClass({
        render: function() {
            return (
                <div>TODO: Login</div>
            );
        }
    });

    module.exports = Login;
})();
