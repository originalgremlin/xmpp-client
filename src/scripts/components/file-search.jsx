(function(undefined) {
    'use strict';

    var React = require('react');

    var FileSearch = React.createClass({
        render: function() {
            return (
                <div>TODO: File search</div>
            );
        }
    });

    module.exports = FileSearch;
})();
